# 喜宝 TV 2.4.3 · 电视播放兼容与遥控器修复

- 播放时遥控器上键切下一台、下键切上一台。过滤长按重复事件及 400 毫秒内的异常重复信号，避免按一下连续跳台。频道列表内仍按上下方向正常浏览。
- 电视自动播放模式新增独立 LibVLC 3.7.6 引擎：原播放器出错或 15 秒未能输出画面时，尝试兼容播放器硬件优先模式，再尝试软件解码。成功的引擎在本次使用期间按频道记忆。
- 电视频道面板上方新增“播放设置”：可选自动兼容、原播放器、兼容播放器硬件优先、兼容播放器软件解码。盒子的视频本来已使用硬件解码；新增引擎同时更换流解析与解码路径。
- 保留 FFmpeg 音频解码；兼容播放器关闭 HDMI 压缩音频直通，使用 PCM 输出。
- 切台、停止、撤销授权和退出时取消过期播放回调。失败重试有上限，不再仅在收到解码错误后才尝试其他引擎。

手机继续使用原播放引擎和现有触屏操作。使用原签名，可覆盖安装，保留授权、服务器与收藏。无需更新服务器。

软件解码可绕过部分盒子固件问题，但高分辨率频道的流畅度取决于盒子性能。模拟器测试不能替代当贝 H3 的真实网络、硬件解码和 HDMI 实测。

验证结果：22 项本地回归测试通过；Android 7（x86）和 Android 15（x86_64）分别通过 6 项设备测试，包括 MP2 / AAC / AC3 / EAC3 音频解码，以及兼容播放器硬件优先、软件解码配置的 H.264 + MP2 测试视频输出。设备测试运行：https://github.com/billconan2017/xibao-iptv/actions/runs/35090860380 。64 位原生库已核对 16 KB ELF 对齐，发布签名与旧版一致。
