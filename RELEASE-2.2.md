# 喜宝 TV 2.2.0

配合后端 2.2.0 使用。安装包同时支持 Android 手机和电视，最低 Android 7（API 24），没有按 ARM32/ARM64 限制安装。

- 手机处理状态栏与屏幕安全区域；竖屏视频卡片下常驻当前节目和进度；横竖屏切换保持播放器。
- 顶部返回、节目单、点播、横竖屏（电视为收藏）、退出按钮。系统返回在直播时提示退出，在频道列表返回播放。
- 每台安装随机设备密钥。升级后在后台家庭设备重新允许一次，覆盖安装继续保留身份。请同时更新后端。
- 当前与下一节目、节目预告、时间进度；无数据时提示配置 EPG。直播进度不能拖动回看。
- 自有苹果 CMS JSON 点播源：换源、搜索、翻页、选线路、选集；视频控制条暂停和拖动进度。

更新使用与 2.1.0 相同的发布签名。授权可避免陌生设备从平台获得源地址，但视频仍是设备直连源，不是具备并发限制的流媒体网关。

自动化检查：Android 9/15 启动与手机交互、电视启动焦点、设备密钥保存、EPG 进度与旋转；运行 lint 与 release 构建。尚未在极光 5C、当贝 H3 实机验证，不保证所有直播源的编码均可解码。
