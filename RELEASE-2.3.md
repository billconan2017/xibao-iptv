# 喜宝 TV 2.3.0 · 手机与电视分开适配

## 手机

- 竖屏为 16:9 视频、紧凑节目信息和频道列表；横屏画面铺满可用区域，保持原始比例。
- 随系统自动旋转，移除会锁住方向的横竖屏按钮。请开启手机系统的“自动旋转”。旋转/调整窗口使用同一个播放器，不重新连接直播。
- 点画面只显示/隐藏播放控件，约 3.5 秒后自动收起。频道通过按钮打开；横屏时是右侧列表。
- 手机频道与节目单使用 14sp 基础字号，电视仍保留 24sp 频道字号。字号继续尊重系统无障碍字体设置。
- 顶部仅返回、频道标题和更多菜单；点播、收藏、服务器设置、退出收纳到更多菜单。
- 节目单改为有时间、当前节目高亮和关闭按钮的滚动面板。暂停后进入后台再回来保持暂停。

## 电视盒子

- 增加电视功能和触摸屏检测，普通 Android 系统的无触摸盒子也进入遥控器布局。
- 服务器设置可选择“自动识别 / 手机触控 / 电视遥控”，兼容上报错误硬件标记的盒子。
- 保留上下换台、确认/菜单打开频道、返回关闭列表、再次返回提示退出。看电视时隐藏顶部菜单。
- 允许播放器在首选解码器失败时尝试设备提供的其他解码器；不新增 FFmpeg，不保证设备不支持的编码能播放。

## 安装与验证

使用原发布签名，可覆盖安装 2.2.0，保留服务器地址、设备密钥、收藏及授权身份。兼容后端 2.2.0；新版 Web 布局随后端 2.3.0 镜像提供。

自动化覆盖 Android 7/9/15 的电视遥控器操作和授权拦截、普通无触摸盒子识别、Android 9/15 手机启动，以及原生视图的竖横屏往返、控件自动隐藏、点击画面不打开频道列表和节目单渲染。执行 release 签名构建和 lint。

极光 5C、当贝 H3 尚未连接实机，实际音视频解码与厂商遥控器仍需实测。

设计参考：[Media3 PlayerView](https://developer.android.com/media/media3/ui/playerview)、[Material 自适应布局](https://developer.android.com/codelabs/adaptive-material-guidance)。
