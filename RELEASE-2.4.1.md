# 喜宝 TV 2.4.1 · 修复部分直播频道无声

问题来自音频格式兼容性：排查到无声频道使用 MPEG-1 Layer II（MP2），原有声音正常的频道使用 AAC。旧 APK 仅依赖 Android 系统解码器，部分手机无法解码 MP2。

本版内置与 Media3 1.11.0 匹配的 FFmpeg 音频解码库，优先解码 MP2 / AAC 等音频后输出 PCM；视频仍使用原硬件解码。保留 2.4.0 的手机布局、全屏切换和电视遥控操作。新增 ARM32、ARM64、x86、x86_64 原生库，按 16 KB 页面对齐构建。

覆盖安装即可，服务器地址、收藏、设备授权身份不变。后端不需要升级，群晖不需要修改或转码直播源。

源码、固定版本的构建脚本及第三方许可证均在仓库内。音频库使用 AndroidX 官方 FFmpeg 桥接代码和固定版本 FFmpeg 源码构建。构建说明见 android/decoder-ffmpeg/README.md；发布构建会检查四种架构的音频库，防止再次漏打包。

验证包括：Android 7 / 9 / 15 的 13 项原有测试；Android 15 模拟器实际加载原生库并将合成 MP2、AAC、AC3、EAC3 测试音频解码成非静音 PCM；APK 签名、架构及对齐检查。尚未在用户的小米手机和两款盒子上实测，本版需覆盖安装后复核原来无声的频道。
